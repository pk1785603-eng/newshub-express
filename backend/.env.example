# ========================================
# 24x7 News Time Backend Environment
# Copy this file to .env and fill values
# ========================================

# Server Configuration
PORT=3001
NODE_ENV=production

# MySQL Database Configuration (Hostinger)
# Get these from Hostinger > Databases > MySQL
DB_HOST=localhost
DB_PORT=3306
DB_USER=your_database_username
DB_PASSWORD=your_database_password
DB_NAME=your_database_name

# Admin Authentication
# Generate a secure password hash using: node -e "console.log(require('bcryptjs').hashSync('your_password', 10))"
ADMIN_PASSWORD_HASH=$2a$10$your_hashed_password_here
JWT_SECRET=your_super_secret_jwt_key_change_this_in_production

# Frontend URL (for CORS)
# In production, this will be your main domain
FRONTEND_URL=https://yourdomain.com

# API Configuration
API_PREFIX=/api
